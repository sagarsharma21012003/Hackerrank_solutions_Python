from collections import Counter

# From an iterable
c = Counter(['a', 'b', 'c', 'a', 'b', 'a'])
# From a mapping
c = Counter({'a': 3, 'b': 2, 'c': 1})
# With keyword arguments
c = Counter(a=3, b=2, c=1)
